import React from 'react'
import ReactDOM from 'react-dom'
import App from './App'
import './index.css'
import 'bootstrap/dist/css/bootstrap.min.css'

ReactDOM.render(
  <App />,
  document.getElementById('root')
)

// en notes cambiar la base url de heroku al localhost/api/notes - probar
// en app crear dos valores de estado-username y password -donde se creara un formulario y donde el usuario pondra el username y su password
// crear en la carp services un arch. login -ir ahi
